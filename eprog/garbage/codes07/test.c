#include <stdio.h>
#include <assert.h>
#include <stdlib.h>

double* scanVector(int n) {

	int i = 0;
	// Pointer vector anlegene und null init
	double* vector != NULL; 
	assert(n > 0);
	
	// Legen benötigten Speicher an
	vector = mallo(n*sizeof(vector))
	assert(vector != NULL);


}

double* scanMatrix(int n, int m) {
	assert(n >= 0);
	assert(m >= 0);

}

int main() {

	int* x = NULL;
	printf("%p\n",x);
	printf("%d\n",*x);
	double* vector = scanVector(int n);
}